论文《Structure-Aware Spatially Adaptive Mixed-Order Variational Model for Image Smoothing》的代码
## 使用方法
1. 将所有文件放在**同一文件夹**，MATLAB 切换到该目录。
2. 打开 `demo.m`，修改图片读取路径：
   % 相对路径（图片和demo同目录）
   img = imread("你的图片名.jpg");
   % 绝对路径示例(Windows)
   % img = imread("D:\xxx\test.png");
